import { minuteHoure } from '../../../helpers/minute-houre';
import { module, test } from 'qunit';

module('Unit | Helper | minute houre');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = minuteHoure(42);
  assert.ok(result);
});
